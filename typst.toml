[package]
name = "luminar-cv"
version = "0.1.0"
entrypoint = "template/cv.typ"
authors = ["Florian Vogt"]
license = "MIT"
description = "A clean, modular CV template with FontAwesome icons, customizable colors, and support for academic publications."
categories = ["cv"]
keywords = ["cv", "resume", "academic", "fontawesome"]

[template]
path = "template/"
entrypoint = "cv.typ"
thumbnail = "thumbnail.png"